import styled from "styled-components";

const Wrapper = styled.div`
`;

const Socials = styled.div`
    display: flex;
    justify-content: center;
    gap: 15px;
    margin: 30px 0;
`

export default { Wrapper, Socials };
