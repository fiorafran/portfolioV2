import styled from "styled-components";
import { onlyMobile } from "../../utils/devices";

const Projects = styled.div`
  width: 100%;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
`;

const LinkWrapper = styled.div`
  display: flex;
  justify-content: center;
  margin: 20px;
`

export default { Projects, LinkWrapper };
